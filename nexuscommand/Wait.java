package frc.robot.nexuscommand;
import frc.robot.*;

public class Wait extends drive{


    public Wait(double seconds, Driver driveSystem)
    {
        super(0,0,seconds,driveSystem);
    }

    
}
